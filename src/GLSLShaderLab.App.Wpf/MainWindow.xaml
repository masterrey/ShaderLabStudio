<Window x:Class="GLSLShaderLab.App.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:wf="clr-namespace:System.Windows.Forms.Integration;assembly=WindowsFormsIntegration"
        mc:Ignorable="d"
        Title="GLSL Shader Lab Studio"
        Height="900"
        Width="1500"
        Loaded="OnLoaded"
        Closing="OnClosing"
        PreviewKeyDown="OnPreviewKeyDown"
        WindowStartupLocation="CenterScreen">
    <DockPanel Background="{DynamicResource PrimaryBackgroundBrush}">
        <!-- Toolbar Row 1: File & Compile -->
        <ToolBar Name="MainToolBar" DockPanel.Dock="Top" Height="Auto" Padding="4" ToolBar.OverflowMode="Never">
            <DockPanel Width="Auto" LastChildFill="False">
                <!-- Left side: File, Compile, Playback -->
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Left" VerticalAlignment="Center">
                    <Button Name="OpenButton" Content="📁 Open" Click="OpenButton_Click" Padding="8,4" Margin="2"/>
                    <Button Name="SaveButton" Content="💾 Save" Click="SaveButton_Click" Padding="8,4" Margin="2"/>
                    <Button Content="Save As…" Click="SaveAsButton_Click" Padding="8,4" Margin="2"/>
                    <Separator Style="{DynamicResource {x:Static ToolBar.SeparatorStyleKey}}" Margin="4,0"/>
                    <Button Name="CompileButton" Content="⚙️ Compile" Click="CompileButton_Click" Padding="8,4" Margin="2"/>
                    <CheckBox Name="AutoCompileCheckBox" Content="Auto" Checked="AutoCompileCheckBox_OnChanged" Unchecked="AutoCompileCheckBox_OnChanged" Margin="4,0,0,0" VerticalAlignment="Center"/>
                    <Separator Style="{DynamicResource {x:Static ToolBar.SeparatorStyleKey}}" Margin="4,0"/>
                    <Button Name="PlayPauseButton" Content="▶️ Play" Click="PlayPauseButton_Click" Padding="8,4" Margin="2"/>
                    <Button Name="ResetTimeButton" Content="⏱️ Reset" Click="ResetTimeButton_Click" Padding="8,4" Margin="2"/>
                    <Button Name="FullscreenButton" Content="🖥️ Full" Click="FullscreenButton_Click" Padding="8,4" Margin="2"/>
                </StackPanel>

                <!-- Right side: Theme Toggle -->
                <Button Name="ThemeToggleButton" Content="🌓 Theme" Click="ThemeToggleButton_Click" Padding="10,4" Margin="2" DockPanel.Dock="Right" Background="{DynamicResource TertiaryBackgroundBrush}" Foreground="{DynamicResource PrimaryForegroundBrush}"/>
            </DockPanel>
        </ToolBar>

        <!-- Toolbar Row 2: 3D Settings & Templates -->
        <ToolBar Name="OptionsToolBar" DockPanel.Dock="Top" Height="Auto" Padding="4" ToolBar.OverflowMode="Never">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="3D Mode:" VerticalAlignment="Center" Margin="4,0"/>
                <ComboBox Name="RenderModeComboBox" Width="80" SelectionChanged="RenderModeComboBox_SelectionChanged" Padding="4" VerticalAlignment="Center">
                    <ComboBoxItem Content="2D" IsSelected="True"/>
                    <ComboBoxItem Content="3D"/>
                </ComboBox>
                <TextBlock Text="Model:" VerticalAlignment="Center" Margin="12,0,4,0"/>
                <ComboBox Name="ModelComboBox" Width="120" SelectionChanged="ModelComboBox_SelectionChanged" Padding="4" VerticalAlignment="Center"/>
                <Button Name="ResetCameraButton" Content="🎯 Reset Cam" Click="ResetCameraButton_Click" Padding="8,4" Margin="4,2"/>
                <Separator Style="{DynamicResource {x:Static ToolBar.SeparatorStyleKey}}" Margin="4,0"/>
                <TextBlock Text="Template:" VerticalAlignment="Center" Margin="4,0"/>
                <ComboBox Name="TemplateComboBox" Width="120" SelectionChanged="TemplateComboBox_SelectionChanged" Padding="4" VerticalAlignment="Center"/>
            </StackPanel>
        </ToolBar>

        <StatusBar Name="MainStatusBar" DockPanel.Dock="Bottom" Height="28" Padding="4">
            <StatusBarItem>
                <TextBlock Name="StatusTextBlock" Text="Ready" Foreground="{DynamicResource PrimaryForegroundBrush}"/>
            </StatusBarItem>
            <Separator/>
            <StatusBarItem>
                <TextBlock Name="FpsTextBlock" Text="FPS: 0" Foreground="{DynamicResource SecondaryForegroundBrush}"/>
            </StatusBarItem>
            <StatusBarItem><TextBlock Name="FileSyncStatusTextBlock" Text="Autosave: 10 min • edição externa: ativa" Foreground="{DynamicResource SecondaryForegroundBrush}"/></StatusBarItem>
        </StatusBar>

        <Grid Name="MainContentGrid" Background="{DynamicResource PrimaryBackgroundBrush}">
            <Grid.RowDefinitions>
                <RowDefinition Name="PreviewRowDefinition" Height="*"/>
                <RowDefinition Name="DiagnosticsRowDefinition" Height="200"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Name="EditorColumnDefinition" Width="1.2*"/>
                <ColumnDefinition Name="PreviewColumnDefinition" Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid Name="EditorPaneGrid" Grid.Row="0" Grid.Column="0" Margin="8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="📝 Shader Editor" FontSize="14" FontWeight="Bold" Margin="0,0,0,8" Foreground="{DynamicResource AccentBrush}"/>

                <TabControl Name="ShaderEditorsTabControl" Grid.Row="1" Background="{DynamicResource SecondaryBackgroundBrush}" Foreground="{DynamicResource PrimaryForegroundBrush}">
                    <TabItem Header="Fragment Shader" Background="{DynamicResource TabBackgroundBrush}" Foreground="{DynamicResource TabForegroundBrush}">
                        <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Background="{DynamicResource EditorBackgroundBrush}">
                            <Grid Margin="4">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Border Grid.Column="0" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,1,0" Padding="0">
                                    <ScrollViewer Name="EditorLineNumbersScroller"
                                                  VerticalScrollBarVisibility="Hidden"
                                                  HorizontalScrollBarVisibility="Hidden"
                                                  Padding="0">
                                        <TextBlock Name="EditorLineNumbers"
                                                   FontFamily="Consolas"
                                                   FontSize="14"
                                                   Padding="8,10,8,0"
                                                   Background="{DynamicResource SecondaryBackgroundBrush}"
                                                   Foreground="{DynamicResource SecondaryForegroundBrush}"
                                                   TextAlignment="Right"/>
                                    </ScrollViewer>
                                </Border>
                                <RichTextBox Name="EditorTextBox"
                                             Grid.Column="1"
                                             FontFamily="Consolas"
                                             FontSize="14"
                                             VerticalScrollBarVisibility="Auto"
                                             HorizontalScrollBarVisibility="Auto"
                                             PreviewMouseWheel="EditorTextBox_PreviewMouseWheel"
                                             PreviewKeyDown="EditorTextBox_PreviewKeyDown"
                                             TextChanged="EditorTextBox_TextChanged"
                                             Background="{DynamicResource EditorBackgroundBrush}"
                                             Foreground="{DynamicResource EditorForegroundBrush}"/>
                            </Grid>
                        </Border>
                    </TabItem>
                    <TabItem Name="VertexEditorTab" Header="Vertex Shader (3D)" Background="{DynamicResource TabBackgroundBrush}" Foreground="{DynamicResource TabForegroundBrush}">
                        <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Background="{DynamicResource EditorBackgroundBrush}">
                            <Grid Margin="4">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Border Grid.Column="0" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,1,0" Padding="0">
                                    <ScrollViewer Name="VertexEditorLineNumbersScroller"
                                                  VerticalScrollBarVisibility="Hidden"
                                                  HorizontalScrollBarVisibility="Hidden"
                                                  Padding="0">
                                        <TextBlock Name="VertexEditorLineNumbers"
                                                   FontFamily="Consolas"
                                                   FontSize="14"
                                                   Padding="8,10,8,0"
                                                   Background="{DynamicResource SecondaryBackgroundBrush}"
                                                   Foreground="{DynamicResource SecondaryForegroundBrush}"
                                                   TextAlignment="Right"/>
                                    </ScrollViewer>
                                </Border>
                                <RichTextBox Name="VertexEditorTextBox"
                                             Grid.Column="1"
                                             FontFamily="Consolas"
                                             FontSize="14"
                                             VerticalScrollBarVisibility="Auto"
                                             HorizontalScrollBarVisibility="Auto"
                                             PreviewMouseWheel="EditorTextBox_PreviewMouseWheel"
                                             PreviewKeyDown="EditorTextBox_PreviewKeyDown"
                                             TextChanged="EditorTextBox_TextChanged"
                                             Background="{DynamicResource EditorBackgroundBrush}"
                                             Foreground="{DynamicResource EditorForegroundBrush}"/>
                            </Grid>
                        </Border>
                    </TabItem>
                </TabControl>
            </Grid>

            <Grid Name="PreviewPaneGrid" Grid.Row="0" Grid.Column="1" Margin="8,8,8,0">
                <Grid.RowDefinitions>
                    <RowDefinition Name="PreviewHeaderRowDefinition" Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Name="PreviewChannelsRowDefinition" Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Name="PreviewTitleTextBlock" Text="👁️ Preview" FontWeight="Bold" FontSize="14" Margin="0,0,0,8" Foreground="{DynamicResource AccentBrush}"/>
                <Border Name="PreviewBorder" Grid.Row="1" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="2" CornerRadius="4" Background="{DynamicResource SecondaryBackgroundBrush}">
                    <wf:WindowsFormsHost Name="PreviewHost"/>
                </Border>

                <UniformGrid Name="ChannelButtonsGrid" Grid.Row="2" Columns="4" Margin="0,8,0,0" HorizontalAlignment="Stretch">
                    <Button Content="Load iChannel0" Click="LoadChannel0_Click" Margin="2" Padding="6,4"/>
                    <Button Content="Load iChannel1" Click="LoadChannel1_Click" Margin="2" Padding="6,4"/>
                    <Button Content="Load iChannel2" Click="LoadChannel2_Click" Margin="2" Padding="6,4"/>
                    <Button Content="Load iChannel3" Click="LoadChannel3_Click" Margin="2" Padding="6,4"/>
                </UniformGrid>
            </Grid>

            <Grid Grid.Row="1" Grid.ColumnSpan="2" Margin="8" Name="DiagnosticsGroupBox">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <DockPanel Margin="0,0,0,8">
                    <Button Name="ClearLogButton" DockPanel.Dock="Right" Content="Clear" ToolTip="Limpar o log de diagnósticos" Click="ClearLogButton_Click"/>
                    <Button Name="GoToErrorButton" DockPanel.Dock="Right" Content="Ir para o erro" IsEnabled="False" Click="GoToErrorButton_Click"/>
                    <TextBlock Name="CompileSummaryTextBlock" Text="Diagnósticos de compilação" FontSize="14" FontWeight="Bold" TextWrapping="Wrap" VerticalAlignment="Center"/>
                </DockPanel>

                <Border Grid.Row="1" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="2" Background="{DynamicResource EditorBackgroundBrush}">
                    <TextBox Name="DiagnosticsTextBox"
                         FontFamily="Consolas"
                         FontSize="14"
                         IsReadOnly="True"
                         TextWrapping="Wrap"
                         AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Auto"
                         Padding="8"
                         Background="{DynamicResource EditorBackgroundBrush}"
                         Foreground="{DynamicResource EditorForegroundBrush}"/>
                </Border>
            </Grid>
        </Grid>
    </DockPanel>
</Window>

